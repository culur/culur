---
'@culur/config-eslint': minor
---

Support **tests** directory naming convention

Add `^__tests__` to filename rule exclusions to accommodate directory name checks in updated `eslint-plugin-unicorn`.
