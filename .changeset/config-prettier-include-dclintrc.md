---
'@culur/config-prettier': patch
---

Include `.dclintrc` in the default ignore list.

Ensures Docker Compose Linter config files are treated like other tool configs when generating Prettier overrides.
