---
'@culur/logger': minor
---

- Feat: Implement `TasksSimple`, a simplified version of `Tasks`.
  - `TasksSimple` only accepts `Task` children and always renders in a grid layout.
  - Added `Tasks.tasksSimple()` method to create `TasksSimple` instances.
