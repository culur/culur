import { defineHasPackages } from './defineHasPackages';

export { defineHasPackages } from './defineHasPackages';

export const {
  defaultPackages,
  updateDefaultPackages,
  hasSass, //
  hasTailwind,
  hasVue,
} = defineHasPackages();
